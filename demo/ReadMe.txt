The webpage now has a functionality that allows the user to upload json file by dragging and dropping the file into the drag zone, but it has to be hosted on a webserver.

I used XAMPP accomplishing this feature.